node_modules
dist
coverage
.DS_Store
.env
docker-compose.override.yml
.pnpm-store
*.log
.local
backups/
logs/
# Prisma client output is regenerated at build time (Dockerfile + CI both run
# `prisma generate`), and the generated engine binaries are platform-specific
# (windows .dll / linux .so), so the whole generated tree stays out of git.
apps/api/src/generated/
